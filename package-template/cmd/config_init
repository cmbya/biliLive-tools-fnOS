#!/bin/bash
STATE_FILE="${TRIM_PKGVAR}/config_was_running"
if "$(dirname "$0")/main" status >/dev/null 2>&1; then
  printf '1\n' > "$STATE_FILE"
  "$(dirname "$0")/main" stop >/dev/null 2>&1 || true
else
  printf '0\n' > "$STATE_FILE"
fi
exit 0
